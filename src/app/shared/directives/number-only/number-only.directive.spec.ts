import {NumberOnlyDirective} from './number-only.directive';

xdescribe('NumberOnlyDirective', () => {
  it('should create an instance', () => {
    const directive = new NumberOnlyDirective();
    expect(directive).toBeTruthy();
  });
});
