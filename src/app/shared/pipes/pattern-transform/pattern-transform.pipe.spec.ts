import { PatternTransformPipe } from './pattern-transform.pipe';

describe('PatternTransformPipe', () => {
  it('create an instance', () => {
    const pipe = new PatternTransformPipe();
    expect(pipe).toBeTruthy();
  });
});
